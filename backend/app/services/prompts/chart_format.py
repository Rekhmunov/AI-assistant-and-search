"""Инструкции для inline-графиков в ответах (блок ```chart, GlosixChart v1)."""

from __future__ import annotations

ANSWER_CHART_FORMAT = """
Графики и диаграммы (интерфейс Glosix):
- Если пользователь просит график, диаграмму, гистограмму, визуализацию, chart/plot — кратко поясни текстом (1–2 предложения), затем ОБЯЗАТЕЛЬНО один блок ```chart с валидным JSON (только JSON внутри, без комментариев и markdown).
- Не рисуй ASCII-графики, псевдографику и «картинки из символов».
- Числа — только из запроса, прикреплённого файла, источников [n] или истории диалога; не выдумывай данные.
- Схема GlosixChart v1:
{
  "type": "bar",
  "title": "Заголовок",
  "labels": ["Янв", "Фев", "Мар"],
  "series": [{"name": "Выручка", "values": [12, 15, 18]}],
  "xLabel": "Месяц",
  "yLabel": "млн ₽"
}
- type: только "bar", "line" или "pie".
- series: от 1 до 5 рядов; labels и values в каждом ряду одной длины; не больше 30 точек.
- Для pie — один ряд в series (доли или абсолютные значения по labels).
- Подписи title, xLabel, yLabel, name — на русском."""
